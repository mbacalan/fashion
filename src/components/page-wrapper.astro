---
import Header from "./header.astro";

interface Props {
  active: "home" | "qa" | "about" | "consultation";
  contentClass?: string;
  mainClass?: string;
  mainId?: string;
}

const {
  active,
  contentClass = "flex flex-col gap-12",
  mainClass = "",
  mainId,
} = Astro.props as Props;

const mainClasses = ["flex-1 px-6 py-10 sm:px-10", mainClass]
  .filter(Boolean)
  .join(" ");

const containerClasses = ["mx-auto w-full max-w-6xl", contentClass]
  .filter(Boolean)
  .join(" ");
---

<div class="flex min-h-screen flex-col">
  <Header active={active} />
  <main id={mainId} class={mainClasses}>
    <div class={containerClasses}>
      <slot />
    </div>
  </main>
</div>
